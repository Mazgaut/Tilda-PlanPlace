async function create_xl(send, imgs) {

    await check_xls_lib()

    let modules = get_modules();

    workbook = new ExcelJS.Workbook();
    workbook.creator = "test";
    workbook.lastModifiedBy = "test";
    workbook.created = new Date();
    workbook.modified = new Date();


    let scspecs = sc.get_specs(true);


    let textHeaderH1 = {size: 13, bold: true};
    let textHeaderH2 = {size: 12, bold: true};
    let textHeaderH3 = {size: 11, bold: true};
    let textInfo = {size: 11, bold: false, italic: true};

    let details_sheet = workbook.addWorksheet("Информация о модулях");

    details_sheet.addRow([]);

    details_sheet.addRow([lang['general_information']]).font = textHeaderH2;
    details_sheet.addRow([]);

    let show_price = custom_settings.info_show_price == undefined ? true : custom_settings.info_show_price;
    let show_icons = constructor_settings.specs_icons == undefined ? true : constructor_settings.specs_icons;

    const specSumKeys = typeof specSumKeysOrderedForExport === 'function'
        ? specSumKeysOrderedForExport(scspecs.sum)
        : Object.keys(scspecs.sum || {});

    for (let ski = 0; ski < specSumKeys.length; ski++) {
        const key = specSumKeys[ski];
        const item = scspecs.sum[key];

        console.log(key, item);

        if (key == 'wall') {
            details_sheet.addRow([lang['wall_header']]).font = textHeaderH3;

            let rowHeader = [
                lang['material'],
                lang['title'],
                lang['code'],
                lang['sizes'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) rowHeader.push(
                lang['price'],
            );

            details_sheet.addRow(rowHeader);

            for (const [i_key, obj] of Object.entries(item)) {
                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.key_name,
                    (obj.variant_name || obj.material_name),
                    (obj.variant_code || obj.material_code),
                    obj.width + 'x' + obj.height + ' мм',
                    //round_decimals_2(obj.total_count) + ' ' + lang['units_' + obj.price_type],
                    round_decimals_2(obj.total_count),
                    lang['units_' + obj.price_type],
                )
                if (show_price) row.push(
                    round_decimals_2(obj.total_price),
                );

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                const added = details_sheet.addRow(row)
                if (obj.is_compound_header) {
                    added.font = Object.assign({}, (added.font || {}), { bold: true });
                }
            }

            details_sheet.addRow([])
        }

        if (key == 'butts') {
            details_sheet.addRow([lang['edge']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['thickness'],
                lang['width'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader);

            for (const [i_key, obj] of Object.entries(item)) {
                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.name,
                    obj.code,
                    obj.thickness + ' мм',
                    obj.height + ' мм',
                    round_decimals_2(obj.total_count),
                    lang['units_' + obj.price_type],
                )
                if (show_price) row.push(round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'butts_facade') {
            details_sheet.addRow([lang['facades_edge']]).font = textHeaderH3;

            let rowHeader = [
                lang['material'],
                lang['facade'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader);

            for (const [i_key, obj] of Object.entries(item)) {
                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.mat_name,
                    obj.facade_name,
                    round_decimals_2(obj.total_count),
                    lang['units_' + obj.price_type],
                )
                if (show_price) row.push(round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'facade') {

            details_sheet.addRow([lang['facades']]).font = textHeaderH3;

            const sizeHeader = Object.values(item).some(obj => obj.show_size);

            let rowHeader = [
                lang['material'],
                lang['title'],
                lang['code'],
            ];

            if (sizeHeader) {
                rowHeader.push(lang['sizes']);
            }

            rowHeader.push(lang['count_short']);
            rowHeader.push(lang['units_of']);

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader);

            for (const [i_key, obj] of Object.entries(item)) {
                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.mat_name,
                    obj.name,
                    obj.mat_code || obj.code,
                )

                if (sizeHeader) {
                    row.push(
                        obj.show_size 
                            ? round_decimals_2(obj.size.x) + 'x' + round_decimals_2(obj.size.y) + ' мм' 
                            : ''
                    );
                }

                row.push(round_decimals_2(obj.total_count));
                row.push(lang['units_' + obj.price_type]);

                if (show_price) row.push(round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'facade_glass') {
            details_sheet.addRow([lang['glass_materials']]).font = textHeaderH3;

            const sizeHeader = Object.values(item).some(obj => obj.show_size);

            let rowHeader = [
                lang['material'],
                lang['code'],
            ];

            if (sizeHeader) {
                rowHeader.push(lang['sizes']);
            }

            rowHeader.push(lang['count_short']);
            rowHeader.push(lang['units_of']);

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader);

            for (const [i_key, obj] of Object.entries(item)) {
                let row = []
                row.push(
                    obj.mat_name,
                    obj.mat_code || obj.code,
                )

                if (sizeHeader) {
                    row.push(
                        obj.show_size 
                            ? obj.width + 'x' + obj.height + ' мм' 
                            : ''
                    );
                }

                row.push(round_decimals_2(obj.total_count));
                row.push(lang['units_' + obj.price_type]);

                if (show_price) row.push(round_decimals_2(obj.total_price));
                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'tabletop') {
            details_sheet.addRow([lang['tabletop']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['sizes'],
                lang['common_sizes'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader)


            for (const [i_key, obj] of Object.entries(item)) {
                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.name,
                    obj.code,
                    obj.width + 'x' + obj.height + ' мм',
                    (obj.price_type === 'm' || obj.price_type === 'm2' || obj.price_type === 'm3') ?
                    round_decimals_2(obj.total_width) + ' ' + lang['units_' + obj.price_type] :
                    round_decimals_2(obj.total_width_m) + ' ' + lang['units_' + 'm'],
                    round_decimals_2(obj.total_count),
                    lang['units_' + obj.price_type],
                )

                if (show_price) row.push(round_decimals_2(obj.total_price));
                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'bort') {
            details_sheet.addRow([lang['tabletop_plints']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['sizes'],
                lang['common_sizes'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader)

            for (const [i_key, obj] of Object.entries(item)) {
                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.name,
                    obj.code,
                    obj.width + 'x' + obj.height + ' мм',
                    (obj.price_type === 'm' || obj.price_type === 'm2' || obj.price_type === 'm3') ?
                        round_decimals_2(obj.total_width) + ' ' + lang['units_' + obj.price_type] :
                        round_decimals_2(obj.total_width_m) + ' ' + lang['units_' + 'm'],
                    round_decimals_2(obj.total_count),
                    lang['units_' + obj.price_type],
                )
                if (show_price) row.push(round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'bort_acc') {
            details_sheet.addRow([lang['bort_acc'] || 'Комплектующие плинтуса']).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) {
                rowHeader.push(lang['price']);
            }

            details_sheet.addRow(rowHeader);

            for (const obj of Object.values(item)) {
                let row = [
                    obj.name,
                    obj.code,
                    round_decimals_2(obj.total_count != null ? obj.total_count : obj.count),
                    lang['units_' + (obj.price_type || 'p')],
                ];

                if (show_price) {
                    row.push(round_decimals_2(obj.total_price != null ? obj.total_price : (obj.price * (obj.count || 1))));
                }

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row);
            }

            details_sheet.addRow([]);
        }

        if (key == 'cokol_facade') {
            details_sheet.addRow([lang['cokol_facade_linked']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['cokol_spec_height'],
                lang['cokol_spec_total_length'],
                lang['common_sizes'],
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader);

            for (const [i_key, obj] of Object.entries(item)) {
                let row = [];
                row.push(
                    obj.name,
                    obj.mat_code || obj.code,
                    obj.height + ' ' + lang['mm'],
                    round_cokol_decimals(obj.total_length_m) + ' ' + lang['units_m'],
                    round_cokol_decimals(obj.total_width) + ' ' + lang['units_m2'],
                );

                if (show_price) row.push(round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row);
            }

            details_sheet.addRow([]);
        }

        if (key == 'cokol') {
            details_sheet.addRow([lang['cokol']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['sizes'],
                lang['common_sizes'],
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader)

            for (const [i_key, obj] of Object.entries(item)) {
                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.name,
                    obj.code,
                    obj.width + 'x' + obj.height + ' мм',
                    format_cokol_spec_common_size(obj, lang),
                )

                if (show_price) row.push(round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'cokol_cuts') {
            details_sheet.addRow([lang['cokol_cuts'] || 'Разрезы цоколей']).font = textHeaderH3;

            details_sheet.addRow([
                lang['title'],
                lang['code'],
                lang['sizes'],
                lang['count_short'],
            ]);

            for (const obj of Object.values(item)) {
                details_sheet.addRow([
                    obj.name,
                    obj.code,
                    obj.size_x + 'x' + obj.size_z + ' мм',
                    round_decimals_2(obj.total_count),
                ]);
            }

            details_sheet.addRow([]);
        }

        if (key == 'cokol_top') {
            details_sheet.addRow([lang['cokol_top']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['sizes'],
                lang['common_sizes'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader)

            for (const [i_key, obj] of Object.entries(item)) {
                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.name,
                    obj.code,
                    obj.width + 'x' + obj.height + ' мм',
                    (obj.price_type === 'm' || obj.price_type === 'm2') ?
                        round_cokol_decimals(obj.total_width) + ' ' + lang['units_' + obj.price_type] :
                        round_cokol_decimals(obj.total_width_m) + ' ' + lang['units_' + 'm'],
                    round_cokol_decimals(obj.total_count),
                    lang['units_' + obj.price_type],
                )

                if (show_price) row.push(round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'cokol_side_acc') {
            details_sheet.addRow([lang['cokol_side_acc'] || 'Комплектующие для цоколя']).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) {
                rowHeader.push(lang['price']);
            }

            details_sheet.addRow(rowHeader);

            for (const obj of Object.values(item)) {
                let row = [
                    obj.name,
                    obj.code,
                    (obj.price_type === 'p'
                        ? (obj.total_count != null ? obj.total_count : obj.count)
                        : round_cokol_decimals(obj.total_count != null ? obj.total_count : obj.count)),
                    lang['units_' + (obj.price_type || 'p')],
                ];

                if (show_price) {
                    row.push(round_decimals_2(obj.total_price != null ? obj.total_price : (obj.price * (obj.count || 1))));
                }

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row);
            }

            details_sheet.addRow([]);
        }

        if (key == 'cornice') {
            details_sheet.addRow([lang['cornice']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['common_sizes'],
                lang['cornice_width'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader)

            for (const [i_key, obj] of Object.entries(item)) {
                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.name,
                    obj.code,
                    obj.total_width + ' м',
                    obj.width + ' м',
                    round_decimals_2(obj.total_count),
                    lang['units_' + obj.price_type],
                )

                if (show_price) row.push(round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'item') {
            details_sheet.addRow([lang['accessories_furniture']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader)

            for (const [i_key, obj] of Object.entries(item)) {
                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.name,
                    obj.code,
                    round_decimals_2(obj.total_count),
                    lang['units_' + obj.price_type],
                )

                if (show_price) row.push((obj.is_compound_header && (parseFloat(obj.total_price) || 0) <= 0) ? (lang['compound'] || 'Составная') : round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'washers') {
            details_sheet.addRow([lang['washers']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['count_short'],
                lang['units_of'],
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader)

            const washerExportRows = groupWashersSpecForExport(item);
            for (let wi = 0; wi < washerExportRows.length; wi++) {
                const obj = washerExportRows[wi];
                let row = []
                row.push(
                    obj.name,
                    obj.code,
                    round_decimals_2(obj.count),
                    lang['units_p'],
                )

                if (show_price) row.push(round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        // Модули коллекции с переопределённой ценой (позиции прайс-листа коллекции)
        if (key == 'collection_module') {
            details_sheet.addRow([lang['collection_price_modules']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['collection'],
                lang['count_short']
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader)

            for (const obj of Object.values(item)) {
                let row = []
                row.push(
                    obj.name,
                    obj.code,
                    obj.collection_name || '',
                    round_decimals_2(obj.total_count),
                )

                if (show_price) row.push(round_decimals_2(obj.total_price));

                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }

        if (key == 'module' && Object.values(item).find((x) => x.price)) {
            details_sheet.addRow([lang['module']]).font = textHeaderH3;

            let rowHeader = [
                lang['title'],
                lang['code'],
                lang['count_short']
            ];

            if (show_price) rowHeader.push(lang['price']);

            details_sheet.addRow(rowHeader)

            for (const [i_key, obj] of Object.entries(item)) {
                if (!(obj.price)) {
                    continue;
                }

                console.log(i_key, obj);
                let row = []
                row.push(
                    obj.name,
                    obj.code,
                    round_decimals_2(obj.total_count),
                )

                if (show_price) row.push(round_decimals_2(obj.total_price));

                // подпись коллекции/каталога: без неё разделённые строки выглядят одинаково
                apply_spec_scope_suffix_to_row(row, obj);
                details_sheet.addRow(row)
            }

            details_sheet.addRow([])
        }
    }

    if(project_settings.comments && project_settings.comments.length > 0){
        details_sheet.addRow([lang['project_comments'] + ':']).font = textHeaderH3;
        for (let i = 0; i < project_settings.comments.length; i++) {
           details_sheet.addRow([project_settings.comments[i].text]);
        }
        details_sheet.addRow([]);
    }

    const servicesCartsForTotal = get_services_carts_for_report();
    for (let c = 0; c < servicesCartsForTotal.length; c++) {
        const cart = servicesCartsForTotal[c];
        details_sheet.addRow([cart.label]).font = textHeaderH2;
        details_sheet.addRow([]);
        details_sheet.addRow([lang['title'] || 'Наименование', lang['price'] || 'Цена']).font = textHeaderH3;
        let cartTotal = 0;
        for (const it of cart.items) {
            details_sheet.addRow([it.name, round_decimals_2(it.price)]);
            cartTotal += it.price;
        }
        details_sheet.addRow([]);
        details_sheet.addRow(['', (lang['total_cost'] || 'Итого') + ': ' + round_decimals_2(cartTotal)]).font = textInfo;
        details_sheet.addRow([]);
    }

    if (show_price && scspecs.mode !== 'object') {
        const discount = parseFloat(project_settings.discount) || 0;
        const productWithDiscount = parseFloat(scspecs.total_price) || 0;
        const productWithoutDiscount = scspecs.total_price_without_discount != null && scspecs.total_price_without_discount !== undefined ? parseFloat(scspecs.total_price_without_discount) : (discount > 0 ? productWithDiscount * 100 / (100 - discount) : productWithDiscount);
        const servicesTotal = parseFloat(scspecs.total_services_price) || 0;
        const totalWithoutDiscount = productWithoutDiscount + servicesTotal;
        const totalWithDiscount = productWithDiscount + servicesTotal;
        const discountAmount = productWithoutDiscount - productWithDiscount;
        const labelTotal = (lang['total_cost'] || 'Общая стоимость');
        if (discount > 0) {
            const labelWithoutDiscount = (lang['total_cost_project_without_discount'] || 'Общая стоимость без скидки');
            const labelWithDiscount = (lang['total_cost_project_with_discount'] || 'Общая стоимость со скидкой');
            details_sheet.addRow(['', '', '', '', '', labelWithoutDiscount + ': ', round_decimals_2(totalWithoutDiscount)]).font = textInfo;
            details_sheet.addRow(['', '', '', '', '', labelWithDiscount + ': ', round_decimals_2(totalWithDiscount)]).font = textInfo;
            details_sheet.addRow(['', '', '', '', '', (lang['discount_label'] || 'Скидка') + ': ' + discount + '% (' + round_decimals_2(discountAmount) + ')']).font = textInfo;
            if (servicesCartsForTotal.length) {
                const discountNotAppliedTo = (lang['discount_not_applied_to'] || '* Скидка не распространяется на: ') + servicesCartsForTotal.map(c => c.label).join(', ');
                details_sheet.addRow(['', '', '', '', '', discountNotAppliedTo]).font = textInfo;
            }
        } else {
            details_sheet.addRow(['', '', '', '', '', labelTotal + ': ', round_decimals_2(totalWithDiscount)]).font = textInfo;
        }
    }

    details_sheet.addRow([])

    if (scspecs.mode !== 'object') {
        for (const [i, obj] of Object.entries(scspecs.all.module || {})) {
            if (!obj.is_root || obj.element_type === "__cokol_plate") {
                continue;
            }

            let show_price2 = show_price && obj.show_price;

            console.log(i, obj);

            if (obj?.ctx?.sum !== undefined) {
                details_sheet.addRow([lang['module'] + ':' + obj.name,]).font = textHeaderH2;

                if (show_icons && obj.icon && obj.icon !== '') {
                    let img64 = await convert_image_to_base64(obj.icon);
                    let size = await get_img_width(obj.icon);
                    let img = workbook.addImage({
                        base64: img64,
                        extension: 'jpg',
                    });

                    details_sheet.addImage(img, {
                        tl: {col: 8, row: details_sheet.lastRow._number - 1},
                        ext: {width: 100, height: 100 / (size.width / size.height)},
                    });
                }

                for (const [key, item] of Object.entries(obj.ctx?.sum || {})) {

                    if (key == 'wall') {
                        details_sheet.addRow([lang['wall_header']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['material'],
                            lang['title'],
                            lang['code'],
                            lang['sizes'],
                            lang['count_short'],
                            lang['units_of'],
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        for (const [i_key, obj] of Object.entries(item)) {
                            console.log(i_key, obj);
                            let row = []
                            row.push(
                                obj.key_name,
                                (obj.variant_name || obj.material_name),
                                (obj.variant_code || obj.material_code),
                                obj.width + 'x' + obj.height + ' мм',
                                round_decimals_2(obj.total_count),
                                lang['units_' + obj.price_type],
                            )
                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            const added2 = details_sheet.addRow(row)
                            if (obj.is_compound_header) {
                                added2.font = Object.assign({}, (added2.font || {}), { bold: true });
                            }
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'butts') {
                        details_sheet.addRow([lang['edge']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['title'],
                            lang['code'],
                            lang['thickness'],
                            lang['width'],
                            lang['count_short'],
                            lang['units_of'],
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        for (const [i_key, obj] of Object.entries(item)) {
                            console.log(i_key, obj);
                            let row = []
                            row.push(
                                obj.name,
                                obj.code,
                                obj.thickness + ' мм',
                                obj.height + ' мм',
                                round_decimals_2(obj.total_count),
                                lang['units_' + obj.price_type],
                            )
                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'butts_facade') {
                        details_sheet.addRow([lang['facades_edge']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['material'],
                            lang['facade'],
                            lang['count_short'],
                            lang['units_of']
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        for (const [i_key, obj] of Object.entries(item)) {
                            console.log(i_key, obj);
                            let row = []
                            row.push(
                                obj.mat_name,
                                obj.facade_name,
                                round_decimals_2(obj.total_count),
                                lang['units_' + obj.price_type],
                            )
                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'facade') {
                        details_sheet.addRow([lang['facades']]).font = textHeaderH3;

                        const sizeHeader = Object.values(item).some(obj => obj.show_size);

                        let rowHeader = [
                            lang['material'],
                            lang['title'],
                            lang['code'],
                        ];

                        if (sizeHeader) {
                            rowHeader.push(lang['sizes']);
                        }

                        rowHeader.push(lang['count_short']);
                        rowHeader.push(lang['units_of']);

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        for (const [i_key, obj] of Object.entries(item)) {
                            console.log(i_key, obj);
                            let row = []
                            row.push(
                                obj.mat_name,
                                obj.name,
                                obj.mat_code || obj.code,
                            )

                            if (sizeHeader) {
                                row.push(
                                    obj.show_size 
                                        ? round_decimals_2(obj.size.x) + 'x' + round_decimals_2(obj.size.y) + ' мм' 
                                        : ''
                                );
                            }

                            row.push(round_decimals_2(obj.total_count));
                            row.push(lang['units_' + obj.price_type]);

                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'cokol_facade') {
                        details_sheet.addRow([lang['cokol_facade_linked']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['title'],
                            lang['code'],
                            lang['cokol_spec_height'],
                            lang['cokol_spec_total_length'],
                            lang['common_sizes'],
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader);

                        for (const [i_key, obj] of Object.entries(item)) {
                            let row = [];
                            row.push(
                                obj.name,
                                obj.mat_code || obj.code,
                                obj.height + ' ' + lang['mm'],
                                round_cokol_decimals(obj.total_length_m) + ' ' + lang['units_m'],
                                round_cokol_decimals(obj.total_width) + ' ' + lang['units_m2'],
                            );

                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            details_sheet.addRow(row);
                        }

                        details_sheet.addRow([]);
                    }

                    if (key == 'facade_glass') {
                        details_sheet.addRow([lang['glass_materials']]).font = textHeaderH3;

                        const sizeHeader = Object.values(item).some(obj => obj.show_size);

                        let rowHeader = [
                            lang['material'],
                            lang['code'],
                        ];

                        if (sizeHeader) {
                            rowHeader.push(lang['sizes']);
                        }

                        rowHeader.push(lang['count_short']);
                        rowHeader.push(lang['units_of']);

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader);

                        for (const [i_key, obj] of Object.entries(item)) {
                            let row = []
                            row.push(
                                obj.mat_name,
                                obj.mat_code || obj.code,
                            )

                            if (sizeHeader) {
                                row.push(
                                    obj.show_size 
                                        ? obj.width + 'x' + obj.height + ' мм' 
                                        : ''
                                );
                            }

                            row.push(round_decimals_2(obj.total_count));
                            row.push(lang['units_' + obj.price_type]);

                            if (show_price2) row.push(round_decimals_2(obj.total_price));
                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'tabletop') {
                        details_sheet.addRow([lang['tabletop']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['title'],
                            lang['code'],
                            lang['sizes'],
                            lang['common_sizes'],
                            lang['count_short'],
                            lang['units_of']
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        for (const [i_key, obj] of Object.entries(item)) {
                            console.log(i_key, obj);
                            let row = []
                            row.push(
                                obj.name,
                                obj.code,
                                obj.width + 'x' + obj.height + ' мм',
                                (obj.price_type === 'm' || obj.price_type === 'm2' || obj.price_type === 'm3') ?
                                    round_decimals_2(obj.total_width) + ' ' + lang['units_' + obj.price_type] :
                                    round_decimals_2(obj.total_width_m) + ' ' + lang['units_' + 'm'],
                                round_decimals_2(obj.total_count),
                                lang['units_' + obj.price_type],
                            )

                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'bort') {
                        details_sheet.addRow([lang['tabletop_plints']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['title'],
                            lang['code'],
                            lang['sizes'],
                            lang['common_sizes'],
                            lang['count_short'],
                            lang['units_of'],
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        for (const [i_key, obj] of Object.entries(item)) {
                            console.log(i_key, obj);
                            let row = []
                            row.push(
                                obj.name,
                                obj.code,
                                obj.width + 'x' + obj.height + ' мм',
                                (obj.price_type === 'm' || obj.price_type === 'm2' || obj.price_type === 'm3') ?
                                    round_decimals_2(obj.total_width) + ' ' + lang['units_' + obj.price_type] :
                                    round_decimals_2(obj.total_width_m) + ' ' + lang['units_' + 'm'],
                                round_decimals_2(obj.total_count),
                                lang['units_' + obj.price_type],
                            )

                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'bort_acc') {
                        details_sheet.addRow([lang['bort_acc'] || 'Комплектующие плинтуса']).font = textHeaderH3;

                        let rowHeader = [
                            lang['title'],
                            lang['code'],
                            lang['count_short'],
                            lang['units_of'],
                        ];

                        if (show_price2) {
                            rowHeader.push(lang['price']);
                        }

                        details_sheet.addRow(rowHeader);

                        for (const obj of Object.values(item)) {
                            let row = [
                                obj.name,
                                obj.code,
                                round_decimals_2(obj.total_count != null ? obj.total_count : obj.count),
                                lang['units_' + (obj.price_type || 'p')],
                            ];

                            if (show_price2) {
                                row.push(round_decimals_2(obj.total_price != null ? obj.total_price : (obj.price * (obj.count || 1))));
                            }

                            details_sheet.addRow(row);
                        }

                        details_sheet.addRow([]);
                    }

                    if (key == 'cokol') {
                        details_sheet.addRow([lang['cokol']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['title'],
                            lang['code'],
                            lang['sizes'],
                            lang['common_sizes'],
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        for (const [i_key, obj] of Object.entries(item)) {
                            console.log(i_key, obj);
                            let row = []
                            row.push(
                                obj.name,
                                obj.code,
                                obj.width + 'x' + obj.height + ' мм',
                                format_cokol_spec_common_size(obj, lang),
                            )

                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'cokol_cuts') {
                        details_sheet.addRow([lang['cokol_cuts'] || 'Разрезы цоколей']).font = textHeaderH3;

                        details_sheet.addRow([
                            lang['title'],
                            lang['code'],
                            lang['sizes'],
                            lang['count_short'],
                        ]);

                        for (const obj of Object.values(item)) {
                            details_sheet.addRow([
                                obj.name,
                                obj.code,
                                obj.size_x + 'x' + obj.size_z + ' мм',
                                round_decimals_2(obj.total_count),
                            ]);
                        }

                        details_sheet.addRow([]);
                    }

                    if (key == 'cokol_top') {
                        details_sheet.addRow([lang['cokol_top']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['title'],
                            lang['code'],
                            lang['sizes'],
                            lang['common_sizes'],
                            lang['count_short'],
                            lang['units_of'],
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        for (const [i_key, obj] of Object.entries(item)) {
                            console.log(i_key, obj);
                            let row = []
                            row.push(
                                obj.name,
                                obj.code,
                                obj.width + 'x' + obj.height + ' мм',
                                (obj.price_type === 'm' || obj.price_type === 'm2') ?
                                    round_cokol_decimals(obj.total_width) + ' ' + lang['units_' + obj.price_type] :
                                    round_cokol_decimals(obj.total_width_m) + ' ' + lang['units_' + 'm'],
                                round_cokol_decimals(obj.total_count),
                                lang['units_' + obj.price_type],
                            )

                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'cornice') {
                        details_sheet.addRow([lang['cornice']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['title'],
                            lang['code'],
                            lang['common_sizes'],
                            lang['cornice_width'],
                            lang['count_short'],
                            lang['units_of'],
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        for (const [i_key, obj] of Object.entries(item)) {
                            console.log(i_key, obj);
                            let row = []
                            row.push(
                                obj.name,
                                obj.code,
                                obj.total_width + ' м',
                                obj.width + ' м',
                                round_decimals_2(obj.total_count),
                                lang['units_' + obj.price_type],
                            )

                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'item') {
                        details_sheet.addRow([lang['accessories_furniture']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['title'],
                            lang['code'],
                            lang['count_short'],
                            lang['units_of'],
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        for (const [i_key, obj] of Object.entries(item)) {
                            console.log(i_key, obj);
                            let row = []
                            row.push(
                                obj.name,
                                obj.code,
                                round_decimals_2(obj.total_count),
                                lang['units_' + obj.price_type],
                            )

                            if (show_price2) row.push((obj.is_compound_header && (parseFloat(obj.total_price) || 0) <= 0) ? (lang['compound'] || 'Составная') : round_decimals_2(obj.total_price));

                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }

                    if (key == 'washers') {
                        details_sheet.addRow([lang['washers']]).font = textHeaderH3;

                        let rowHeader = [
                            lang['title'],
                            lang['code'],
                            lang['count_short'],
                            lang['units_of'],
                        ];

                        if (show_price2) rowHeader.push(lang['price']);

                        details_sheet.addRow(rowHeader)

                        const washerExportRows = groupWashersSpecForExport(item);
                        for (let wi = 0; wi < washerExportRows.length; wi++) {
                            const obj = washerExportRows[wi];
                            let row = []
                            row.push(
                                obj.name,
                                obj.code,
                                round_decimals_2(obj.count),
                                lang['units_p'],
                            )

                            if (show_price2) row.push(round_decimals_2(obj.total_price));

                            details_sheet.addRow(row)
                        }

                        details_sheet.addRow([])
                    }
                }

                if(pdf_settings.report_modules_comments_list !== false){
                    if (obj.comments && String(obj.comments).trim() !== '') {
                        details_sheet.addRow([lang['module_comments'] + ':']).font = textHeaderH3;
                        details_sheet.addRow([obj.comments]);
                        details_sheet.addRow([]);
                    }
                }

                if (show_price2) details_sheet.addRow(['', '', '', '', '', lang['price_module'] + ': ', round_decimals_2(obj.price)]).font = textInfo;

                details_sheet.addRow([]);
                details_sheet.addRow([]);
            }

        }
    }

    let images;

    if (!imgs) {
        images = get_images();
    } else {
        images = imgs;
    }

    if (images.common_view) {
        let sheet = workbook.addWorksheet("Общий вид");
        let img = workbook.addImage({
            base64: images.common_view,
            extension: 'jpg',
        });
        let size = await get_img_width(images.common_view)
        sheet.addImage(img, {
            tl: {col: 0, row: 0},
            ext: {width: size.width, height: size.height}
        });
    }

    if (images.edges_view) {
        let sheet = workbook.addWorksheet("Вид по контору");
        let img = workbook.addImage({
            base64: images.edges_view,
            extension: 'jpg',
        });
        let size = await get_img_width(images.edges_view)
        sheet.addImage(img, {
            tl: {col: 0, row: 0},
            ext: {width: size.width, height: size.height}
        });
    }

    if (images.comms_only_view) {
        let sheet = workbook.addWorksheet("Вид коммуникаций");
        let img = workbook.addImage({
            base64: images.comms_only_view,
            extension: 'jpg',
        });
        let size = await get_img_width(images.comms_only_view)
        sheet.addImage(img, {
            tl: {col: 0, row: 0},
            ext: {width: size.width, height: size.height}
        });
    }

    if (images.top_view) {
        let sheet = workbook.addWorksheet("Вид сверху");
        let img = workbook.addImage({
            base64: images.top_view,
            extension: 'jpg',
        });
        let size = await get_img_width(images.top_view)
        sheet.addImage(img, {
            tl: {col: 0, row: 0},
            ext: {width: size.width, height: size.height}
        });
    }

    if (images.front_view) {
        let sheet = workbook.addWorksheet("Стена 1");
        let img = workbook.addImage({
            base64: images.front_view,
            extension: 'jpg',
        });
        let size = await get_img_width(images.front_view)
        sheet.addImage(img, {
            tl: {col: 0, row: 0},
            ext: {width: size.width, height: size.height}
        });
    }

    if (images.right_view) {
        let sheet = workbook.addWorksheet("Стена 2");
        let img = workbook.addImage({
            base64: images.right_view,
            extension: 'jpg',
        });
        let size = await get_img_width(images.right_view)
        sheet.addImage(img, {
            tl: {col: 0, row: 0},
            ext: {width: size.width, height: size.height}
        });

    }

    if (images.back_view) {
        let sheet = workbook.addWorksheet("Стена 3");
        let img = workbook.addImage({
            base64: images.back_view,
            extension: 'jpg',
        });
        let size = await get_img_width(images.back_view)
        sheet.addImage(img, {
            tl: {col: 0, row: 0},
            ext: {width: size.width, height: size.height}
        });
    }

    if (images.left_view) {
        let sheet = workbook.addWorksheet("Стена 4");
        let img = workbook.addImage({
            base64: images.left_view,
            extension: 'jpg',
        });
        let size = await get_img_width(images.left_view)
        sheet.addImage(img, {
            tl: {col: 0, row: 0},
            ext: {width: size.width, height: size.height}
        });
    }

    if (send) {
        return workbook.xlsx.writeBuffer();
    } else {
        workbook.xlsx.writeBuffer().then(function (data) {
            var blob = new Blob([data], {type: "application/xlsx"});
            var downloadUrl = window.URL.createObjectURL(blob);
            var a = document.createElement("a");
            a.href = downloadUrl;
            a.download = 'planplace.xlsx';
            document.body.appendChild(a);
            a.click();
        });
    }


}